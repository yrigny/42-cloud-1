- hosts: cloud1
  become: true

  roles:
    - firewall
    # Docker must be installed before the stack can be rendered and started.
    - docker
    # Service roles prepare their own build contexts before the stack is deployed.
    - mariadb
    - wordpress
    - nginx
    # The webapp role renders the compose file and starts the Inception stack.
    - webapp
